# 결정

- 학습용 폼 고지와 실제 전송되지 않는다는 성공 문구를 유지했다. 개인정보를 서버나 저장소에 전송하지 않는다.
- 입력 도중 다른 미입력 필드의 오류를 생성하지 않는다. 제출 시 전체 검증하고 자동 완성 값도 읽는다.
- 표시 문자열은 textContent이며 사용자 입력을 HTML로 삽입하지 않는다.
- 오류를 해결한 입력의 aria-invalid를 false로 돌리고, 성공 후 입력을 수정하면 이전 성공 표시를 지운다.
